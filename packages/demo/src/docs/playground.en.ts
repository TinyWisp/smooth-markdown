import type { MessageMap } from '@smooth-markdown/core'

const message: MessageMap = {
  playground: {
    langEn: 'English language support',
    langZhCN: 'Chinese language support',
    lang: 'custom language support',
    overlayScrollbars: 'scrollbars based on OverlayScrollbars',
    affixToolbar: 'affix the toolbar on the top of the window',
    autoHeight: 'make the editor\'s height fit its content',
    math: 'math rendering',
    tocSpy: 'table of contents',
    syncScrollbars: 'sync the scrollbars',
    defaultEditorTheme: 'the default theme of the editable area',
    defaultViewerTheme: 'the default theme of the preview area',
    customCodeBlockRenderer: 'customize the ',
    pasteImage: 'uploading the pasted image and inserting it into the document',
    markdownItPlugins: 'loading markdown-it plugins',
    markdownItOptions: 'setting the options of the markdown-it',
    codemirorExt: "loading codemirror's extensions",
    handleImageClick: 'handling the click events of the images',
    customLinkAttrs: 'custom the attributes of links'
  }
}

export default message